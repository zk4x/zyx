[package]
name = "zyx-opencl"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
zyx-core = { version = "*", path = "../zyx-core" }
opencl-sys = { version = "0.2.8", default-features = false }

[features]
default = ["debug1"]
debug1 = []
