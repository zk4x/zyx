[package]
name = "zyx-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5", default-features = false, features = ["small_rng"] }

[dev-dependencies]
zyx-opencl = { version = "*", path = "../zyx-opencl" }
